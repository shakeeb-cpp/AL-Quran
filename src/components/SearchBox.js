import React from 'react'

const SearchBox = () => {

    // console.log(searchWord)


    return (
        <div className='mb-2'>
            {/* <form className="form d-flex float-start border-0 position-absolute" role="search">
                <input className="form-control border-0  text-bg-secondary w-100 me-2" placeholder='Search a word ...' value={props.searchWord} onChange={(event) => props.setSearchWord(event.target.value)} type="search" aria-label="Search" style={{ boxShadow: "none" }} />
            </form> */}
        </div>
    )

}

export default SearchBox
